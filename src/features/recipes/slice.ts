import { createSlice, type PayloadAction } from "@reduxjs/toolkit";
import { RecipeStateData } from "./types";

const initialState: RecipeStateData = {

}

const recipeSlice = createSlice({
  name: 'recipeSlice',
  initialState,
  reducers: {

  }
})
